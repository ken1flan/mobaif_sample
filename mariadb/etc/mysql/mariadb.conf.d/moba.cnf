[mysqld]
skip-name-resolve
character-set-server = cp932
init-connect="SET NAMES cp932"
innodb_flush_log_at_trx_commit=2
sync_binlog=0
innodb_use_native_aio=0

[mysql]
default-character-set = cp932

[mysqldump]
default-character-set = cp932
