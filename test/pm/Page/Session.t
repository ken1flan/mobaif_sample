use strict;
use utf8;

use MobaConf;
use Test::Spec;
use Page::Main;

describe 'Page::Session' => sub {
  describe 'pageNew' => sub {
    it 'TODO' => sub {
      ok(1);
    };
  };

  describe 'pageCreate' => sub {
    it 'TODO' => sub {
      ok(1);
    };
  };

  describe 'pageDestroy' => sub {
    it 'TODO' => sub {
      ok(1);
    };
  };
};

runtests unless caller;
